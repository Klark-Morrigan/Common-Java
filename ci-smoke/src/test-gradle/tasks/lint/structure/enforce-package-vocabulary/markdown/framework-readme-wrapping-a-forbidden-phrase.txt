# Sample

Prose wraps where the column runs out rather than where the phrase ends, so a
two-word entry has to be found across the break as well as within one line: the
inset ring this package draws is a national
border, and calling it that is the leak.
