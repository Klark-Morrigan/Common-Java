package example.production;

// A word list matched as bare text would report every line here: 'mockup',
// 'mockingbird' and 'faker' each carry a banned word as a prefix and none of
// them says it. A gate that fired on these would be allowlisted into silence,
// which is worse than no gate.
final class Sample {

    void renderMockup() {
        // A mockingbird sings what the faker taught it.
    }
}
