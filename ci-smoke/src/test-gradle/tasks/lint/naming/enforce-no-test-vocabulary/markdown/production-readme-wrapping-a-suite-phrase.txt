# Sample

Prose wraps where the column runs out rather than where the phrase ends, so a
per-line scan would miss half the prose uses of any multi-word entry. The seam
exists so that a widget can be laid out without a unit
test having to describe a box by hand.
