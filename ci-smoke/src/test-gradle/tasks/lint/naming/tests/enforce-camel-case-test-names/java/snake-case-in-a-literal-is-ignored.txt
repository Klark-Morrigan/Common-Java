class Sample {
    // A comment naming a_method(x) the gate must not read.
    @Test
    void readsTheOwnerOffTheMarket() {
        var entityTypeId = "derelict_cryosleeper(";
        readOverrideOf(entityTypeId);
    }
}
